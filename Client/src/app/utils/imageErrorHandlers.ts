export function imageProfileErrorHandler(imageRef: HTMLImageElement) {
    imageRef.src = "images/profile.png";
}

export function imageErrorHandler(imageRef:HTMLImageElement){
    imageRef.src="images/borken-image.png"
}